class EvaluationPlan:

    def __init__(self, g, d, t1, t2, p, K):
        self.g = g
        self.d = d
        self.t1 = t1
        self.t2 = t2
        self.p = p
        self.K = K

#    def EvaluationPlanAlg(self):
# TODO: g_tag, e_tag = AddingEdges(g, p)
# TODO: RP = ResearchPlan(g_tag, self.d, self.t1, self.t2, self.p, self.K)
# TODO: G_R = RP.ResearchPlanAlg()
# TODO: count = 0
# TODO: for edge in e_tag:
    # TODO: if edge not in G_R.edges: count+=1
# TODO: successRate = count/e_tag.size() * 100
# TODO: return successRate





